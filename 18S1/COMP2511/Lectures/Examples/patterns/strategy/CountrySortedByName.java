package patterns.strategy;

import java.util.Comparator;

public class CountrySortedByName implements Comparator<Country>{

	@Override
	public int compare(Country arg0, Country arg1) {
		// Add the code to do comparison by name
		return 0;
	}

}
